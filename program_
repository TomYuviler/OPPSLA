from synthesize import generate_random_condition

class Program:
    def __init__(self, img_dim):
        self.cond_1 = generate_random_condition(img_dim)
        self.cond_2 = generate_random_condition(img_dim)
        self.cond_3 = generate_random_condition(img_dim)
        self.cond_4 = generate_random_condition(img_dim)
